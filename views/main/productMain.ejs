<% layout('layoutPage') -%>

<div class="container">
    <% if (errors.length > 0) { %>
    <div class="alert alert-danger alert-dismissible">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <%= errors %>
    </div>
    <% } %>

    <% if (message.length > 0) { %>
    <div class="alert alert-danger alert-dismissible">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <%= message %>
    </div>
    <% } %>
</div>

<div class="container">
    <div class="row">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="row" id="slidder">
                <div id="myCarousel" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
                        <li data-target="#myCarousel" data-slide-to="1"></li>
                        <li data-target="#myCarousel" data-slide-to="2"></li>
                    </ol>

                    <div class="carousel-inner">
                        <div class="item active">
                            <img src="/assets/images/11.png" alt="">
                        </div>

                        <div class="item">
                            <img src="/assets/images/22.png" alt="">
                        </div>
                        <div class="item">
                            <img src="/assets/images/44.png" alt="">
                        </div>
                    </div>

                    <a class="left carousel-control" href="#myCarousel" data-slide="prev">
                        <span class="glyphicon glyphicon-chevron-left"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="right carousel-control" href="#myCarousel" data-slide="next">
                        <span class="glyphicon glyphicon-chevron-right"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>




<div class="container">
    <div class="row" id="dealsOfDayProducts">
        <Center><h1><strong>Today's Deals</strong></h1></Center>
        <% if( products ) { %>
        <% for ( var i = 0; i < products.length; i++ ) { %>
        <div class="col-md-4">
            <div class="thumbnail">
                <%if (products[i].image) {%>
                <img src="data:image/png;base64, <%= products[i].image %>" height="100" width="100">
                <% } %>
                <div class="caption">
                    <h3>
                        <%= products[i].name %>
                    </h3>
                    <p>
                        <%= products[i].categoryName %>
                    </p>
                    <% if(products[i].subcategoryName) { %>
                    <p>
                        <%= products[i].subcategoryName %>
                    </p>
                    <% } %>
                    <p>
                        <%= products[i].brandName %>
                    </p>
                    <p>TK
                        <%= products[i].unitPrice %>
                    </p>
                </div>
                <a href="/add-to-cart/<%= products[i]._id %>"  class="btn btn-primary btn-md">Add Cart</a>
                <a role="button" class="btn btn-primary btn-md" href="/product/<%= products[i]._id %>">Details</a>
            </div>
        </div>
        <% } %>
        <% } %>
    </div>
    <div class="row" id="exclusiveProduct">
            <Center><h1><strong>Exclusive Products</strong></h1></Center>
        <% if(products) { %>
        <% for ( var i = 0; i < products.length; i++ ) { %>
        <div class="col-md-4">
            <div class="thumbnail">
                <%if (products[i].image) {%>
                <img src="data:image/png;base64, <%= products[i].image %>" height="100" width="100">
                <% } %>
                <div class="caption">
                    <h3>
                        <%= products[i].name %>
                    </h3>
                    <p>
                        <%= products[i].categoryName %>
                    </p>
                    <% if(products[i].subcategoryName) { %>
                    <p>
                        <%= products[i].subcategoryName %>
                    </p>
                    <% } %>
                    <p>
                        <%= products[i].brandName %>
                    </p>
                    <p>TK
                        <%= products[i].unitPrice %>
                    </p>
                </div>
                <a href="/add-to-cart/<%= products[i]._id %>" class="btn btn-primary btn-md">Add Cart</a>
                <a role="button" class="btn btn-primary btn-md" href="/product/<%= products[i]._id %>">Details</a>
            </div>
        </div>
        <% } %>
        <% } %>
    </div>
    <div class="row" id="trendingProduct">
            <Center><h1><strong>Trending Products</strong></h1></Center>
        <% if(products) { %>
        <% for ( var i = 0; i < products.length; i++ ) { %>
        <div class="col-md-4">
            <div class="thumbnail">
                <%if (products[i].image) {%>
                <img src="data:image/png;base64, <%= products[i].image %>" height="100" width="100">
                <% } %>
                <div class="caption">
                    <h3>
                        <%= products[i].name %>
                    </h3>
                    <p>
                        <%= products[i].categoryName %>
                    </p>
                    <% if(products[i].subcategoryName) { %>
                    <p>
                        <%= products[i].subcategoryName %>
                    </p>
                    <% } %>
                    <p>
                        <%= products[i].brandName %>
                    </p>
                    <p>TK
                        <%= products[i].unitPrice %>
                    </p>
                </div>
                <a href="/add-to-cart/<%= products[i]._id %>" class="btn btn-primary btn-md">Add Cart</a>
                <a role="button" class="btn btn-primary btn-md" href="/product/<%= products[i]._id %>">Details</a>
            </div>
        </div>
        <% } %>
        <% } %>
    </div>

    <div class="row" id="searchResults">
            <Center><h1><strong>All Products</strong></h1></Center>
        <% if(products) { %>
        <% for ( var i = 0; i < products.length; i++ ) { %>
        <div class="col-md-4">
            <div class="thumbnail">
                <%if (products[i].image) {%>
                <img src="data:image/png;base64, <%= products[i].image %>" height="100" width="100">
                <% } %>
                <div class="caption">
                    <h3>
                        <%= products[i].name %>
                    </h3>
                    <p>
                        <%= products[i].categoryName %>
                    </p>
                    <% if(products[i].subcategoryName) { %>
                    <p>
                        <%= products[i].subcategoryName %>
                    </p>
                    <% } %>
                    <p>
                        <%= products[i].brandName %>
                    </p>
                    <p>TK
                        <%= products[i].unitPrice %>
                    </p>
                </div>
                <a href="/add-to-cart/<%= products[i]._id %>" class="btn btn-primary btn-md">Add Cart</a>
                <a role="button" class="btn btn-primary btn-md" href="/product/<%= products[i]._id %>">Details</a>
            </div>
        </div>
        <% } %>
        <% } %>

        <ul class="pagination text-center">
            <li>
                <% for(var i = 1; i <= pages; i++ ) { %>
                <% if(i === 1) { %>
            <li><a href="/">
                    <%= i %></a></li>
            <% } else { %>
            <li><a href="/page/<%= i %>">
                    <%= i %></a></li>
            <% } %>
            <% } %>
            </li>
        </ul>

    </div>
</div>