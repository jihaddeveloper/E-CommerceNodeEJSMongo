<% layout('layoutPage') -%>

<div class="container">
  <% if (errors.length > 0) { %>
  <br />
  <br />
  <br />
  <div class="alert alert-danger alert-dismissible">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
    <%= errors %>
  </div>

  <% } %> <% if (message.length > 0) { %>
  <br />
  <br />
  <br />
  <div class="alert alert-success alert-dismissible">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
    <%= message %>
  </div>
  <% } %>
</div>

<div class="container" style="margin-top: 100px">
  <div class="row" id="">
    <Center>
      <h1><strong>My Product</strong></h1>
    </Center>
    <% if( products ) { %> <% for ( var i = 0; i < products.length; i++ ) { %>
    <div class="col-md-4">
      <div class="thumbnail">
        <a role="button" href="">
          <%if (products[i].images) {%>
          <img src="<%= products[i].images[0] %>" height="50%" width="50%" />
          <% } %>
          <div class="caption">
            <h3>
              <%= products[i].name %>
            </h3>
            <p>
              <%= products[i].price %>TK
            </p>
          </div>
          <div class="caption">
            <p>Category: <%= products[i].secondHandProductCategory.name %></p>
            <p>
              Subcategory: <%= products[i].secondHandProductSubCategory.name %>
            </p>
            <p>Brand: <%= products[i].secondHandProductBrand.name %></p>
            <p>Added Date:</p>
            <p>
              <%= moment(products[i].created).format("YYYY-MM-DD HH:mm") %>
            </p>
          </div>
        </a>
        <br />
      </div>
    </div>
    <% } %> <% } %>
  </div>
</div>
